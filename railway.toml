[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["java"]

[build.nixpacks.build]
cmd = "cd backend && javac -cp ./lib/postgresql-42.7.3.jar -d out $(find src -name '*.java')"

[start]
cmd = "cd backend && java -cp out:lib/postgresql-42.7.3.jar com.tuticuenta.server.AppServer"
