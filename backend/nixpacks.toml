[phases.setup]
nixPkgs = ["jdk17_headless", "bash"]

[phases.build]
cmds = [
  "mkdir -p out",
  "javac -cp lib/postgresql-42.7.3.jar -d out $(find src/main/java -name '*.java')"
]

[start]
cmd = "java -cp out:lib/postgresql-42.7.3.jar com.tuticuenta.server.AppServer"
